// 機能の公開スイッチ (2026-09-12)。
//
// 「まだ出さない機能」を 1 箇所で表現するための置き場。
// 個別の画面でバラバラに条件を書くと、必ずどこかに穴が残る (実際に 3 箇所残っていた)。

/**
 * 先生機能を公開するか。
 *
 * **既定は false (未公開)。**
 * false の間は、生徒側・先生側のどちらの入口も出さない:
 *   - ボトムタブの「先生」
 *   - 設定の「先生とつながる」(招待コード入力)
 *   - 「先生を探す」画面 (直リンクも塞ぐ)
 *   - 「先生とのやりとり」画面 (直リンクも塞ぐ)
 *   - アカウントメニューの「先生モードへ」
 *   - 先生側の /teacher 配下すべて
 *
 * すでに先生と紐づいている生徒がいる場合、この値を false にすると
 * その画面にも入れなくなる (データは消えない)。公開時に true にする。
 *
 * 公開する日に必ず見直すこと:
 *   `plan.ts` の getGradingQuota は teacherStudent があれば採点を無制限にしている。
 *   先生機能を出すと、ここが最大の原価漏れになる。
 */
export const TEACHER_FEATURE_ENABLED = false
