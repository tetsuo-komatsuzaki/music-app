"use client"

// Commit 7 (退会機能) で実装。現状はプレースホルダ。
// SettingsClient.tsx の "退会する" ボタンが accountDeletionEnabled=true の時に
// このモーダルを開く想定。

export default function DeleteAccountModal(_props: { open: boolean; onClose: () => void }) {
  return null
}
