node_modules

# env files (secrets)
.env*
!.env.example

.next

# v1.3 B-3 follow-up: Prisma client は schema.prisma から自動生成される派生物。
# build / postinstall 時に `prisma generate` で再生成されるため git 管理から除外。
# (旧 .gitignore の `/generated/prisma` は root 直下を指す誤パターンだった、修正)
app/generated/prisma/
tmp/
__pycache__/
*.pyc
prisma/data/mxl/
prisma/data/mxl_arpeggio/

# MCP
.mcp.json

# === Step A0: モノレポ化に伴う追加除外ルール (v2) ===

# Python virtual env / cache (music-analyzer/, relay-service/)
**/venv/
**/.pytest_cache/
**/.mypy_cache/
**/*.egg-info/
*.pyo

# music-analyzer の楽譜サンプル (著作権配慮、prisma/data/mxl は対象外)
music-analyzer/**/*.mxl
music-analyzer/**/*.musicxml

# OS / IDE
.DS_Store
Thumbs.db
*.swp

# 一時ファイル
*.tmp
*.bak
